# Local
VITE_API_BASE_URL=http://localhost:8084

# Production (set in GitHub Actions secret — baked in at build time)
# VITE_API_BASE_URL=https://calistrack-backend.onrender.com

# GitHub project Pages base (set in workflow; leave unset for local)
# VITE_BASE_PATH=/calistrack/
